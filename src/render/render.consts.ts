export const RenderEventType = {
    pre: 1,
    post: 2
}

export const View = {
    // Fullscreen.
    width: window.innerWidth,
    height: window.innerHeight,

    // Debug window.
    // width: 1024,
    // height: 768,
    
    fieldOfView: 45,

    visibleNear: 0.1,
    visibleFar: 100000
}